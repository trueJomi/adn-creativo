---
import { Image } from "astro:assets"
import { routes } from "../const/routes"
import logo from "../assets/logo.webp"
import Tiktok from "../assets/Tiktok.astro"
import Yt from "../assets/yt.astro"
import Insta from "../assets/Insta.astro"
---
<footer class="bg-primary text-background py-10 font-teko text-2xl">
  <div class="container mx-auto px-4">
    <div class="flex flex-col md:flex-row justify-between items-center gap-8">
      <!-- Logo y descripción -->
      <div class="flex flex-col items-center md:items-start lg:flex-row lg:items-center gap-4 text-center md:text-left">
        <a href="/">
          <Image 
            src={logo} 
            alt="Logo ADN CREATIVO" 
            class="h-16 w-auto" 
            width={100} 
            height={100}
          />
        </a>
        <p class="max-w-xs text-sm">
          ADN CREATIVO es un proyecto que busca impulsar el talento artístico y creativo en la comunidad.
        </p>
      </div>

      <!-- Enlaces de navegación -->
      <div class="">
        <h3 class="font-kulen text-2xl mb-4 text-center font-medium">Enlaces</h3>
        <nav class="grid grid-cols-2 gap-4 lg:grid-cols-3">
          {routes.map((route) => (
            <a href={route.path} class="hover:text-opacity-70 transition-colors text-lg hover:underline">
              {route.name}
            </a>
          ))}
        </nav>
      </div>

      <!-- Redes sociales -->
      <div class="flex flex-col items-center md:items-start">
        <h3 class="font-kulen text-xl mb-4">Síguenos</h3>
        <div class="flex gap-4">
          <a 
            href="https://www.instagram.com/adncreativoart" 
            class="border-2 bg-background text-primary border-primary h-10 w-10 p-2 flex items-center justify-center hover:scale-110 transition-transform"
            aria-label="Instagram"
          >
            <Insta/>
          </a>
          <a 
            href="https://www.tiktok.com/@adncreativoart" 
            class="border-2 bg-background text-primary border-primary h-10 w-10 p-2 flex items-center justify-center hover:scale-110 transition-transform"
            aria-label="TikTok"
          >
            <Tiktok />
          </a>
          <a 
            href="https://www.youtube.com/@ADNCREATIVOART" 
            class="border-2 bg-background text-primary border-primary h-10 w-10 p-2 flex items-center justify-center hover:scale-110 transition-transform"
            aria-label="YouTube"
          >
            <Yt />
          </a>
        </div>
      </div>
    </div>

    <!-- Línea divisoria -->
    <hr class="my-8 border-background border-opacity-20" />
    
    <!-- Copyright -->
    <div class="text-center text-sm">
      <p>&copy; 2025 ADN CREATIVO. Todos los derechos reservados.</p>
        <p class="mt-2">Diseñado por <a href="https://github.com/trueJomi" class="text-secondary hover:underline" target="_blank">@truejomi</a></p>
    </div>
  </div>
</footer>